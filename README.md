# Week 4 Assignment: Grades, Eligibility & Smart Decisions

`welcome.py` defines and uses one `welcome()` function to print three PLP greeting messages.

`toolbox.py` defines `double()`, `is_pass()`, and `greet()` and runs the required tests.

The hardest function to write was `greet()` because it uses a default parameter while also allowing a custom greeting to replace it.
